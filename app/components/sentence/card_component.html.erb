<%= tag.div class: "card mb-2", data: { testid: testid } do %>
  <%= tag.div class: "card-header d-flex justify-content-between align-items-center py-1" do %>
    <%= link_to article_title || t(".default_title"), article_path(article.uuid), class: "text-subtitle text-body-secondary small" %>
    <%= tag.div class: "d-flex gap-3" do %>
      <%= link_to "#{article_path(article.uuid)}/#{sentence.line_number}", class: "text-decoration-none text-muted fw-bold small" do %>
        <%= render IconComponent.new("arrow-right-circle") %> Context
      <% end %>
      <%= link_to token_analysis_analyzer_path(text: sentence.text), class: "text-decoration-none text-muted fw-bold small" do %>
        <%= render IconComponent.new("arrow-right-circle") %> Analyze
      <% end %>
    <% end %>
  <% end %>
  <%= tag.div class: "card-body" do %>
    <%= tag.div class: "card-text" do %>
      <%= decorated_text %>
    <% end %>
    <%= tag.div class: "card-text text-end small text-muted d-flex justify-content-end align-items-center gap-2" do %>
      <%= link_to article.url, class: "text-muted" do %>
        <%= published_year || t(".default_year") %>, <%= article_site_name || t(".default_site_name") %>
      <% end %>
      <%= render Shared::CollapsedClipboardComponent.new(value: article.normalized_url, label: "URL", icon: "link") %>
      <%= render Shared::CollapsedClipboardComponent.new(value: article.uuid, label: "UUID", icon: "hash", placement: :top) %>
    <% end %>
  <% end %>
<% end %>
