<%= tag.ul class: "list-inline d-flex flex-wrap mb-0 gap-2" do %>
  <% collection.collection_tags.each do |collection_tag| %>
    <%= tag.li class: "list-inline-item m-0" do %>
      <%= render Badge::CollectionTagComponent.new(collection_tag) %>
    <% end %>
  <% end %>
<% end %>
