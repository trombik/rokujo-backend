<%= form_with(model: collection_tag) do |form| %>
  <% if collection_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(collection_tag.errors.count, "error") %> prohibited this collection_tag from being saved:</h2>

      <ul>
        <% collection_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
