<%= form_with(model: site_name_correction) do |form| %>
  <% if site_name_correction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(site_name_correction.errors.count, "error") %> prohibited this site_name_correction from being saved:</h2>

      <ul>
        <% site_name_correction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :domain, style: "display: block" %>
    <%= form.text_field :domain %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
